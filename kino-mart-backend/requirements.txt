Django==5.2.13
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.4.0
django-filter==24.3
Pillow

# Production
django-environ==0.14.0   # loads settings from .env / real env vars
whitenoise==6.12.0       # serves collected static files without a separate web server
gunicorn==26.0.0         # production WSGI server (see Procfile)
dj-database-url==3.1.2   # parses DATABASE_URL into DATABASES['default']
psycopg2-binary==2.9.10  # Postgres driver — only needed if you set DATABASE_URL to a postgres:// URL
